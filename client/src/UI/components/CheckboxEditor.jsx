import React, { Fragment } from 'react';

const CheckboxEditor = () => {
    return (
        <Fragment>
            {/* {this.props.error && <div className="inline-error-message"><span className="error">{this.props.error}</span></div>}
            <div className="controls-checkbox">
                <input type="checkbox" className="form-input" id={this.state.id} defaultValue={this.props.value} />
                <label htmlFor={this.state.id}>
                    <span className="label">{this.props.label ? this.props.label : this.props.children}</span>
                </label>
            </div> */}

            
        </Fragment>
    );
};

export default CheckboxEditor;
